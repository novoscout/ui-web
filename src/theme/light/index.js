import colour from "./colour"

const light = {
  desk: {
    backgroundColor: "#fff"
  },
  swiper: {
    frame: {
      backgroundColor: "#fff"
    },
    inner: {
      backgroundColor: "#fff"
    }
  }
}

export default light
export { light }
