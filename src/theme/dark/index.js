import colour from "./colour"

const dark = {
  desk: {
    backgroundColor: "silver"
  },
  swiper: {
    frame: {
      backgroundColor: "#cdcccc"
    },
    inner: {
      backgroundColor: "#decccc"
    }
  }
}

export default dark
export { dark }
