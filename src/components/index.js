import { Desk } from "./Desk"
import { Nav } from "./Nav"
import { NavTitle } from "./NavTitle"
import { Swiper } from "./Swiper"
import { TextLink } from "./TextLink"

const exp = {
  Desk: Desk,
  Nav: Nav,
  NavTitle: NavTitle,
  Swiper: Swiper,
  TextLink: TextLink
}

export default exp
export { Desk, Nav, NavTitle, Swiper, TextLink }
